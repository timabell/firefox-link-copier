{
	"name": "firefox-link-copier",
	"version": "0.0.0-git",
	"type": "module",
	"description": "Firefox extension to copy tab links in multiple formats",
	"author": "Tim Abell <t@0x5.uk>",
	"homepage": "https://github.com/timabell/firefox-link-copier",
	"repository": {
		"type": "git",
		"url": "https://github.com/timabell/firefox-link-copier.git"
	},
	"bugs": {
		"url": "https://github.com/timabell/firefox-link-copier/issues"
	},
	"scripts": {
		"test": "jest",
		"test:watch": "jest --watch",
		"lint": "eslint src/*.js tests/*.js",
		"build": "web-ext build --source-dir=src --artifacts-dir=dist --overwrite-dest",
		"start": "web-ext run --source-dir=src"
	},
	"devDependencies": {
		"jest": "^30.0.5",
		"jest-environment-jsdom": "^30.0.5",
		"eslint": "^9.32.0",
		"web-ext": "^8.9.0"
	},
	"jest": {
		"testEnvironment": "jsdom"
	}
}